# Архитектурные решения

## Push-уведомления для остановок

### Контекст
Пользователям необходимо получать уведомления о приближении транспорта к избранным остановкам, чтобы не пропустить нужный трамвай и не тратить время на постоянную проверку приложения.

### Решение
Реализована система push-уведомлений на базе Web Push API с использованием стандарта VAPID.

### Архитектурные компоненты

#### 1. Backend (Node.js + Express)
**Библиотека**: `web-push`

**Хранилище подписок**:
- Тип: In-memory Map
- Структура: `{ stopId: [{ subscription, notificationMinutes }] }`
- Обоснование: Для MVP достаточно хранения в памяти. В будущем можно мигрировать на Redis или БД.

**API Endpoints**:
- `GET /api/vapid-public-key` - возвращает публичный VAPID ключ для клиента
- `POST /api/stops/:stopId/subscribe` - регистрирует подписку на уведомления для остановки
- `POST /api/stops/:stopId/unsubscribe` - удаляет подписку

**Логика отправки**:
- Интеграция с существующим endpoint `/api/track/:uuid`
- При каждом обновлении данных о трамваях проверяется необходимость отправки уведомлений
- Отправка происходит если время до прибытия ≤ настроенного порога (по умолчанию 3 минуты)
- Защита от спама: cooldown 5 минут между уведомлениями для одного маршрута на одной остановке

**VAPID ключи**:
- Генерируются при первом запуске
- Могут быть переопределены через переменные окружения `PUBLIC_VAPID_KEY` и `PRIVATE_VAPID_KEY`
- Используются для аутентификации сервера при отправке push-уведомлений

#### 2. Frontend (Vanilla JavaScript)

**Service Worker** (`/public/sw.js`):
- Регистрируется при загрузке приложения
- Обрабатывает события `push` - показывает уведомление
- Обрабатывает события `notificationclick` - фокусирует окно приложения

**UI компоненты**:
- Кнопка подписки/отписки на странице остановки
- Визуальная индикация статуса подписки (включено/выключено)
- Статус-сообщения об успешной подписке или ошибках

**Workflow подписки**:
1. Пользователь выбирает остановку
2. Отображается кнопка "Включить уведомления"
3. При клике запрашивается разрешение браузера (`Notification.requestPermission()`)
4. После получения разрешения создается подписка через `PushManager.subscribe()`
5. Подписка отправляется на backend
6. UI обновляется для отображения статуса "включено"

#### 3. Формат уведомлений

**Структура данных**:
```json
{
  "title": "🚊 Трамвай приближается",
  "body": "Трамвай {номер} прибывает через {минуты} мин на {название остановки}",
  "icon": "/icon-192.png",
  "badge": "/badge-72.png",
  "tag": "tram-{номер}-{stopId}",
  "data": {
    "stopId": "uuid",
    "tramNumber": "номер",
    "arrivalMinutes": число
  }
}
```

**Tag**: используется для группировки уведомлений - новое уведомление заменяет старое с тем же tag.

### Альтернативы рассмотренные

#### 1. WebSocket + Server-Sent Events
**Минусы**: 
- Требует постоянного подключения
- Не работает когда вкладка закрыта
- Больше нагрузка на сервер

#### 2. Polling с интервалом
**Минусы**:
- Высокая нагрузка на сервер
- Не работает когда вкладка закрыта
- Большой расход батареи

#### 3. Сторонние сервисы (Firebase Cloud Messaging)
**Минусы**:
- Зависимость от внешнего сервиса
- Дополнительная сложность интеграции
- Возможные проблемы с доступностью в России

### Выбранное решение: Web Push API
**Плюсы**:
- Нативная поддержка браузеров
- Работает даже когда вкладка закрыта
- Стандартный протокол
- Низкая нагрузка на сервер
- Не требует внешних зависимостей

**Минусы**:
- Не поддерживается в Safari на iOS (только в macOS Safari 16+)
- Требует HTTPS (или localhost для разработки)

### Ограничения текущей реализации

1. **Хранилище в памяти**: Подписки хранятся в памяти и теряются при перезапуске сервера
   - **Решение для продакшена**: Использовать Redis или PostgreSQL

2. **Одна подписка на остановку**: Пользователь может иметь только одну активную подписку на каждую остановку
   - **Возможное улучшение**: Поддержка множественных подписок с разными настройками

3. **Фиксированное время уведомления**: По умолчанию 3 минуты
   - **Возможное улучшение**: Настройка времени через UI

4. **Нет персистентности настроек**: При переходе на другую остановку нужно заново подписываться
   - **Возможное улучшение**: Сохранение подписок в localStorage или на сервере

### Безопасность

1. **VAPID**: Использование VAPID ключей предотвращает отправку уведомлений неавторизованными серверами
2. **Cooldown**: Защита от спама уведомлений
3. **Автоочистка**: Невалидные подписки удаляются автоматически
4. **Разрешения**: Требуется явное разрешение пользователя

### Производительность

1. **Проверка подписок**: O(n) где n - количество подписок на остановку
2. **Отправка уведомлений**: Асинхронная, не блокирует основной поток
3. **Cooldown map**: Автоматическая очистка старых записей
4. **Частота проверки**: Привязана к существующему polling интервалу (30 секунд)

### Масштабируемость

**Текущая нагрузка**: Для небольшого количества пользователей (< 1000) текущая реализация достаточна.

**Для роста**:
1. Переход на Redis для хранения подписок
2. Отдельный worker процесс для отправки уведомлений
3. Rate limiting для API endpoints
4. Кеширование данных о транспорте

### Совместимость браузеров

**Поддерживается**:
- Chrome 42+
- Firefox 44+
- Edge 17+
- Opera 29+
- Safari 16+ (только macOS)

**Не поддерживается**:
- Safari на iOS (на момент 2026)
- Internet Explorer

**Graceful degradation**: При отсутствии поддержки кнопка не отображается, приложение работает в обычном режиме.

### Мониторинг и отладка

**Логирование**:
- Успешная отправка уведомлений
- Ошибки при отправке
- Удаление невалидных подписок

**Метрики** (для будущей реализации):
- Количество активных подписок
- Количество отправленных уведомлений
- Rate успешной доставки
- Частота отписок

### Дальнейшее развитие

1. **Персистентность**: Миграция на БД для хранения подписок
2. **Настройки**: UI для настройки времени уведомлений
3. **Множественные подписки**: Возможность подписаться на несколько остановок
4. **История уведомлений**: Просмотр отправленных уведомлений
5. **Тихие часы**: Настройка времени, когда уведомления не отправляются
6. **Группировка**: Умная группировка уведомлений о нескольких трамваях
7. **Analytics**: Отслеживание эффективности уведомлений
