# Server Configuration
PORT=3000

# Vehicles Endpoint Configuration
# Cache TTL for vehicles data in milliseconds (default: 10000 = 10 seconds)
VEHICLES_CACHE_TTL=10000

# Rate Limiting Configuration
# Maximum number of vehicle requests per IP per minute (default: 60)
MAX_VEHICLES_REQUESTS_PER_WINDOW=60
